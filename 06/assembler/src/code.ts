export function code(instruction: Pick<ParseResult, 'comp' | 'dest' |'jump'>): string {
     return ''
}

function dest(mnemonic: string): number {
    
}

function comp(mnemonic: string): number {
    
}

function jump(mnemonic: string): number {
    
}